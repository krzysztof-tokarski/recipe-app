import { ChildLoaderDirective } from './child-loader.directive';

describe('ChildLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
